apiVersion: v1
kind: Service
metadata:
  name: fastapi-gitops
  namespace: dev
  labels:
    app: fastapi-gitops
    environment: dev
spec:
  type: LoadBalancer
  selector:
    app: fastapi-gitops
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
