apiVersion: v1
kind: Service
metadata:
  name: fastapi-gitops
  namespace: prod
  labels:
    app: fastapi-gitops
    environment: prod
spec:
  type: LoadBalancer
  selector:
    app: fastapi-gitops
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
